mobs:
  wolf: {}
  cat:
    enabled: false
    include-tamed-mobs: true # should NOT produce error
  pig:
    enabled: true
    include-tamed-mobs: false # should produce error
  fox:
    enabled: true
    include-tamed-mobs: true # should NOT produce error (even though does not implement Tameable)
