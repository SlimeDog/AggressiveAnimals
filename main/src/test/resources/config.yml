mobs:
  wolf: {}
  cat:
    enabled: false
    include-tamed-mobs: true # should NOT produce error
  pig:
    enabled: true
    include-tamed-mobs: false # should show warning
    attack-only-in-water: false # should show arning
  chicken: {}
  fox:
    enabled: true
    include-tamed-mobs: false # should NOT produce error (even though does not implement Tameable)
  ocelot:
    enabled: true
    include-tamed-mobs: false # should NOT produce error (even though does not implement Tameable)
  axolotl: # something aquatic
    enabled: true
